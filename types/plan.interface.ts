export interface IPlan {
    id: string;
    name: string;
    baseprice: number;
    duration: string;
    isActive: boolean;
    description: string;
    maximumOder: number;
    createdAt: string;
    updatedAt: string;
}
